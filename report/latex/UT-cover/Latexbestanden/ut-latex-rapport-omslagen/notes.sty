\ProvidesPackage{notes}

\RequirePackage{graphicx}
\RequirePackage{float}
\RequirePackage{geometry}
\RequirePackage{fancyhdr}


\def\subtitle#1{\gdef\@subtitle{#1}}
\def\university#1{\gdef\@university{#1}}

\renewcommand{\rmdefault}{phv} % Arial
\renewcommand{\sfdefault}{phv} % Arial


\geometry{a4paper,hmargin=1in,vmargin=0.5in}

\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}

\renewcommand{\@makechapterhead}[1]{%

  {\parindent \z@ \raggedright \normalfont
  %  \hrule                                        % horizontal line
    \vspace{5pt}%                                 % add vertical space
    \ifnum \c@secnumdepth >\m@ne
        \huge \bfseries \scshape \@chapapp\space \thechapter % Chapter number
        \par\nobreak
        \vskip 20\p@
    \fi
    \interlinepenalty\@M
    \Huge \bfseries \scshape #1\par                         % chapter title
    \vspace{5pt}%                                 % add vertical space
   % \hrule                                        % horizontal rule
    \nobreak
    \vskip 40\p@
  }}

\renewcommand{\@makeschapterhead}[1]{%

  {\parindent \z@ \raggedright
    \normalfont
  %  \hrule                                        		% horizontal line
    \vspace{5pt}%                                 	% add vertical space
    \interlinepenalty\@M
    \Huge \bfseries \scshape #1\par	% chapter title
    \vspace{5pt}%                                 	% add vertical space
 %   \hrule                                        		% horizontal line
    \nobreak
    \vskip 40\p@
  }}

%-------------------------- DEFINE TITLE PAGE --------------------------
\renewcommand{\maketitle}{{
	\pagestyle{empty}
	
 	\addtolength{\textwidth}{-1.75in}
   	\addtolength{\oddsidemargin}{1.35in}
  	
	\setlength\textwidth{-3in}
	
	\thiswatermark{				%Put watermark on this page only
    		\put(-190,-885){\includegraphics[]{images/UT3.eps}}
   	}

	\addtolength{\voffset}{-5pt}

	\flushleft

\hspace{6cm}
\begin{minipage}[t][1.5cm][l]{65mm}
\@date
\end{minipage}
\begin{minipage}[l][][l]{5in}
\vspace{2cm}
\textbf{\fontsize{32}{36}\selectfont \MakeTextUppercase{\@title}}\\

\vspace{0.1cm}
\textbf{\fontsize{17}{21}\selectfont \MakeTextUppercase{\@subtitle}}
\end{minipage}

\vspace{1.6cm}
\uppercase{\@author}\\
   \cleardoublepage
}
\pagestyle{fancy}
}

%-------------------------- END TITLE PAGE --------------------------

%% Remove Existing header and footers and replace with below
\fancyhf{}

\renewcommand{\headrulewidth}{0pt}	%no line for top header
\renewcommand{\footrulewidth}{0pt}	%no line for botom header

%\fancyhead[L,L]{\leftmark} 
%\fancyhead[R,R]{\@date}
\fancyfoot[L,L]{} 
\fancyfoot[R,R]{Page \thepage} 

%% For Chapter and Part start pages remove the header information
\fancypagestyle{plain}{% 
	\fancyhead{} %get rid of the headers on plain pages 
	\fancyfoot[L]{} 
	\fancyfoot[R]{Page \thepage}
	%\renewcommand{\headrulewidth}{0pt}
}

\floatstyle{ruled}
\newfloat{example}{H}{loe}
\floatname{example}{Example}
